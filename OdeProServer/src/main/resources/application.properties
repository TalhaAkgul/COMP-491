spring.jpa.hibernate.ddl-auto=update
spring.jpa.database=mysql
spring.datasource.url=jdbc:mysql://localhost:3306/OdeProDB
spring.datasource.username=admin
spring.datasource.password=admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql:true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# The format used for the keystore.
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/Certificates.p12
# The password used to generate the certificate
server.ssl.key-store-password=Softwaring.TPEB
# The alias mapped to the certificate
#server.ssl.key-alias=SOFTWARING
server.ssl.enabled=true
#Accept only HTTPS requests
server.port=8080
#server.address=45.84.207.191
spring.banner.location=classpath:/templates/banner.txt